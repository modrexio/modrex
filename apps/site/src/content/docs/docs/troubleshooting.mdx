---
title: Troubleshooting
description: Solutions to the most common problems with Modrex and your mods.
---

import Callout from '../../../components/docs/Callout.astro'

## Mods not loading in-game

Check that:

- The loader the game needs is installed. For example, PAYDAY 3 loads pak mods only through
  UE4SS. Each game's page under [Supported games](/docs/games/) names its loader.
- The mod's toggle is on.
- A higher-priority mod isn't overriding it. Try moving it lower in the list.
- The mod targets the active game and your installed game version.

## Game not detected on first launch

If Modrex can't find the selected game automatically:

1. Go to **Settings > Game > Game Path** and click **Browse**.
2. Navigate to the game's installation root folder.
3. Click **Select Folder** - Modrex validates and saves the path.

For the Xbox app, Modrex scans every fixed drive letter on your system for the Xbox install path.
This can take a few seconds on the first detection pass.

## "Missing file" badge on a mod

This appears when Modrex's state file references a file or folder that no longer exists on disk.
This happens after manually deleting files or moving your game folder.

Either **Reinstall** the mod to re-download it, or click **Remove** to clear the stale entry. See [Priority and state](/docs/concepts/priority-and-state#missing-files) for how
Modrex tracks this state.

## Archive cannot be installed

If Modrex says it cannot tell where an archive installs, the archive likely belongs inside another
mod or has a layout that does not identify a supported target. Follow the author's instructions
shown in the dialog, then install the files manually if needed.

## Modrex won't launch after a Windows update

Xbox app installs can lose their file permissions after major Windows updates. Run Modrex as
administrator once to restore access, then relaunch normally.

## Log file location

When reporting a bug, always attach the Modrex log file:

| Platform | Path                                  |
| -------- | ------------------------------------- |
| Windows  | %LOCALAPPDATA%\Modrex\logs\Modrex.log |
| Linux    | ~/.local/share/modrex/logs/Modrex.log |

Open it directly from **Settings > Advanced > Logs > Open log file**.

<Callout>
    Get help by filing a [GitHub issue](https://github.com/modrexio/modrex/issues) or posting in the
    [🛠️support](https://discord.com/channels/1508553766025170986/1516892414059610223) forum on
    Discord. Either way, include your OS, launcher, and the log file - issues without logs are much
    harder to diagnose.
</Callout>
